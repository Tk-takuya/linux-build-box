name: build-rizin

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Show runner info
        run: |
          uname -a
          lsb_release -a || cat /etc/os-release
