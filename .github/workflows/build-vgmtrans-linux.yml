name: build-vgmtrans-linux-gui-nondeploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    steps:
      - name: Checkout workflow repo
        uses: actions/checkout@v6

      - name: Clone VGMTrans
        run: |
          set -euxo pipefail
          git clone --recursive https://github.com/vgmtrans/vgmtrans.git vgmtrans-src

      - name: Sanity check source tree
        working-directory: vgmtrans-src
        run: |
          set -euxo pipefail
          echo "PWD=$(pwd)"
          ls -la
          test -f CMakePresets.json
          git submodule status

      - name: Install dependencies
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            ninja-build \
            file \
            xvfb \
            qt6-base-dev \
            qt6-base-private-dev \
            qt6-shadertools-dev \
            libqt6svg6-dev \
            libxcb-cursor-dev \
            libfontconfig1-dev \
            libfreetype-dev \
            libgtk-3-dev \
            libx11-dev \
            libx11-xcb-dev \
            libxcb-glx0-dev \
            libxcb-icccm4-dev \
            libxcb-image0-dev \
            libxcb-keysyms1-dev \
            libxcb-randr0-dev \
            libxcb-render-util0-dev \
            libxcb-shape0-dev \
            libxcb-shm0-dev \
            libxcb-sync-dev \
            libxcb-util-dev \
            libxcb-xfixes0-dev \
            libxcb-xkb-dev \
            libxcb1-dev \
            libxext-dev \
            libxfixes-dev \
            libxi-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-dev \
            libxrender-dev

      - name: Show tool versions
        run: |
          set -euxo pipefail
          cmake --version
          ninja --version
          g++ --version

      - name: Configure
        working-directory: vgmtrans-src
        run: |
          set -euxo pipefail
          test -f CMakePresets.json
          cmake --preset linux-x64 \
            -DENABLE_UI_QT=ON \
            -DENABLE_CLI=ON \
            -DENABLE_SHELL=ON \
            -DINSTALL_SHELL=ON \
            -DNO_QT_DEPLOY=ON \
            -DCMAKE_INSTALL_PREFIX="$GITHUB_WORKSPACE/out"

      - name: Build
        working-directory: vgmtrans-src
        run: |
          set -euxo pipefail
          cmake --build --preset linux-x64-release -j"$(nproc)" --verbose

      - name: Install bundle
        working-directory: vgmtrans-src
        run: |
          set -euxo pipefail
          mkdir -p "$GITHUB_WORKSPACE/out/bin"
          cmake --install build/linux-x64 --config Release --prefix "$GITHUB_WORKSPACE/out" || true

      - name: Locate built binaries
        working-directory: vgmtrans-src
        run: |
          set -euxo pipefail
          echo "== search build tree for candidates =="
          find build/linux-x64 -type f \( -name 'vgmtrans' -o -name 'vgmtrans-cli' -o -name 'vgmtrans-shell' \) | sort || true

      - name: Stage binaries from build tree if missing
        working-directory: vgmtrans-src
        run: |
          set -euxo pipefail
          mkdir -p "$GITHUB_WORKSPACE/out/bin"

          if [ ! -f "$GITHUB_WORKSPACE/out/bin/vgmtrans" ]; then
            GUI_PATH="$(find build/linux-x64 -type f -name 'vgmtrans' | sort | head -n 1 || true)"
            test -n "$GUI_PATH"
            cp -fv "$GUI_PATH" "$GITHUB_WORKSPACE/out/bin/vgmtrans"
          fi

          if [ ! -f "$GITHUB_WORKSPACE/out/bin/vgmtrans-cli" ]; then
            CLI_PATH="$(find build/linux-x64 -type f -name 'vgmtrans-cli' | sort | head -n 1 || true)"
            if [ -n "$CLI_PATH" ]; then
              cp -fv "$CLI_PATH" "$GITHUB_WORKSPACE/out/bin/vgmtrans-cli"
            fi
          fi

          if [ ! -f "$GITHUB_WORKSPACE/out/bin/vgmtrans-shell" ]; then
            SHELL_PATH="$(find build/linux-x64 -type f -name 'vgmtrans-shell' | sort | head -n 1 || true)"
            if [ -n "$SHELL_PATH" ]; then
              cp -fv "$SHELL_PATH" "$GITHUB_WORKSPACE/out/bin/vgmtrans-shell"
            fi
          fi

      - name: Inspect installed bundle
        run: |
          set -euxo pipefail
          echo "== installed/staged files =="
          find "$GITHUB_WORKSPACE/out" -maxdepth 6 -type f | sort
          echo
          echo "== file types =="
          file "$GITHUB_WORKSPACE"/out/bin/* || true

      - name: Show dynamic deps
        run: |
          set -euxo pipefail
          if [ -f "$GITHUB_WORKSPACE/out/bin/vgmtrans" ]; then
            ldd "$GITHUB_WORKSPACE/out/bin/vgmtrans" || true
          fi
          if [ -f "$GITHUB_WORKSPACE/out/bin/vgmtrans-cli" ]; then
            ldd "$GITHUB_WORKSPACE/out/bin/vgmtrans-cli" || true
          fi
          if [ -f "$GITHUB_WORKSPACE/out/bin/vgmtrans-shell" ]; then
            ldd "$GITHUB_WORKSPACE/out/bin/vgmtrans-shell" || true
          fi

      - name: Verify CLI
        run: |
          set -euxo pipefail
          if [ -f "$GITHUB_WORKSPACE/out/bin/vgmtrans-cli" ]; then
            "$GITHUB_WORKSPACE/out/bin/vgmtrans-cli" --help || true
          fi

      - name: Verify shell presence
        run: |
          set -euxo pipefail
          if [ -f "$GITHUB_WORKSPACE/out/bin/vgmtrans-shell" ]; then
            "$GITHUB_WORKSPACE/out/bin/vgmtrans-shell" --help || true
          fi

      - name: Smoke test GUI under Xvfb
        run: |
          set -euxo pipefail
          test -f "$GITHUB_WORKSPACE/out/bin/vgmtrans"
          set +e
          timeout 10s xvfb-run -a "$GITHUB_WORKSPACE/out/bin/vgmtrans" >"$RUNNER_TEMP/vgmtrans_gui.log" 2>&1
          status=$?
          set -e
          cat "$RUNNER_TEMP/vgmtrans_gui.log" || true
          if [ "$status" -ne 0 ] && [ "$status" -ne 124 ]; then
            exit "$status"
          fi

      - name: Package tarball
        run: |
          set -euxo pipefail
          tar -C "$GITHUB_WORKSPACE/out" -czf vgmtrans_linux_x86_64_gui_nondeploy_bundle.tar.gz .

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: vgmtrans_linux_x86_64_gui_nondeploy_bundle
          path: vgmtrans_linux_x86_64_gui_nondeploy_bundle.tar.gz
          if-no-files-found: error
          compression-level: 0
