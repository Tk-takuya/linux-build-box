--- a/src/main/conversion/SF2Conversion.cpp
+++ b/src/main/conversion/SF2Conversion.cpp
@@ -105,27 +105,45 @@

         // Determine the sample number within the rgn's associated SampColl
         size_t realSampNum;
         // If a sample offset is provided, then find the sample number based on this offset.
         // see sampOffset declaration in header file for more info.
         if (rgn->sampOffset != -1) {
           bool bFoundIt = false;
-          for (uint32_t s = 0; s < sampColl->samples.size(); s++) {  //for every sample
-            auto sample = sampColl->samples[s];
-            if (rgn->sampOffset == sample->offset() ||
-                rgn->sampOffset == sample->offset() - sampColl->offset() - sampColl->sampDataOffset) {
-              if (rgn->sampDataLength != -1 && rgn->sampDataLength != sample->dataLength) {
-                continue;
+          const VGMSampColl* matchedSampColl = sampColl;
+          for (const VGMSampColl* candidateSampColl : finalSampColls) {
+            for (uint32_t s = 0; s < candidateSampColl->samples.size(); s++) {  //for every sample
+              auto sample = candidateSampColl->samples[s];
+              if (rgn->sampOffset == sample->offset() ||
+                  rgn->sampOffset == sample->offset() - candidateSampColl->offset() - candidateSampColl->sampDataOffset) {
+                if (rgn->sampDataLength != -1 && rgn->sampDataLength != sample->dataLength) {
+                  continue;
+                }
+
+                realSampNum = s;
+                matchedSampColl = candidateSampColl;
+                bFoundIt = true;
+                break;
               }
-
-              realSampNum = s;
-
-              //samples[m]->loop.loopStart = parInstrSet->aInstrs[i]->aRgns[k]->loop.loopStart;
-              //samples[m]->loop.loopLength = (samples[m]->dataLength) - (parInstrSet->aInstrs[i]->aRgns[k]->loop.loopStart); //[aInstrs[i]->aRegions[k]->sample_num]->dwUncompSize/2) - ((aInstrs[i]->aRegions[k]->loop_point*28)/16); //to end of sample
-              bFoundIt = true;
+            }
+            if (bFoundIt)
               break;
-            }
           }
           if (!bFoundIt) {
             L_ERROR("Failed matching region to a sample with offset {:#x} (Instrset "
                     "{}, Instr {}, Region {})", rgn->sampOffset, inst, i, j);
             realSampNum = 0;
+          } else {
+            sampColl = matchedSampColl;
           }
         }
         // Otherwise, the sample number should be explicitly defined in the rgn.
         else {
           realSampNum = rgn->sampNum;
         }
--- a/src/main/conversion/DLSConversion.cpp
+++ b/src/main/conversion/DLSConversion.cpp
@@ -129,24 +129,36 @@
         // Determine the sample number within the rgn's associated SampColl
         size_t realSampNum;
         // If a sample offset is provided, then find the sample number based on this offset.
         // see sampOffset declaration in header file for more info.
         if (rgn->sampOffset != -1) {
           bool bFoundIt = false;
-          for (uint32_t s = 0; s < sampColl->samples.size(); s++) {             //for every sample
-            if (rgn->sampOffset == sampColl->samples[s]->offset() ||
-                rgn->sampOffset == sampColl->samples[s]->offset() - sampColl->offset() - sampColl->sampDataOffset) {
-              realSampNum = s;
-
-              //samples[m]->loop.loopStart = parInstrSet->aInstrs[i]->aRgns[k]->loop.loopStart;
-              //samples[m]->loop.loopLength = (samples[m]->dataLength) - (parInstrSet->aInstrs[i]->aRgns[k]->loop.loopStart); //[aInstrs[i]->aRegions[k]->sample_num]->dwUncompSize/2) - ((aInstrs[i]->aRegions[k]->loop_point*28)/16); //to end of sample
-              bFoundIt = true;
+          VGMSampColl* matchedSampColl = sampColl;
+          for (VGMSampColl* candidateSampColl : finalSampColls) {
+            for (uint32_t s = 0; s < candidateSampColl->samples.size(); s++) {             //for every sample
+              if (rgn->sampOffset == candidateSampColl->samples[s]->offset() ||
+                  rgn->sampOffset == candidateSampColl->samples[s]->offset() - candidateSampColl->offset() - candidateSampColl->sampDataOffset) {
+                realSampNum = s;
+                matchedSampColl = candidateSampColl;
+                bFoundIt = true;
+                break;
+              }
+            }
+            if (bFoundIt)
               break;
-            }
           }
           if (!bFoundIt) {
             L_ERROR("Failed matching region to a sample with offset {:#x} (Instrset "
                     "{}, Instr {}, Region {})", rgn->sampOffset, inst, i, j);
             realSampNum = 0;
+          } else {
+            sampColl = matchedSampColl;
           }
         }
           // Otherwise, the sample number should be explicitly defined in the rgn.
         else
           realSampNum = rgn->sampNum;
